defaults:
  - sample_flow_hopper

algorithm:
  _target_: src.models.flow.SafeFlowMathcing
  horizon: ${horizon}
  observation_dim: ${obs_dim}
  action_dim: ${act_dim}
  n_timesteps: ${steps}
  loss_type: 'l2'
  action_weight: 10
  loss_discount: 1.0
  loss_weights: null
  env_name: "hopper"
  safe_method: "safeflow"

env_name: hopper
algo_name: safeflow
run_name: sample

device: "cuda:0"

