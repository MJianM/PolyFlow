defaults:
  - time_polyflow_halfcheetah
  - _self_

backbone:
  cons_begin_seq_idx: 0
  share_traj_encoder: true
  time_invariance_cons: true
  ray_shooting_method: 'softmin'
  ray_shooting_beta: 80

eval:
  load_model_path: "outputs/halfcheetah/polyflow_train_softmin80/42_2026-01-12_19-50-17/state_final.pt"
  load_ema: true
  samples: 200   # 计算分布差异时，采样多少个horizon
  # rollout
  n_episodes: 10  # rollout 多少条轨迹
  seed: ${seed}
  is_video: true
  video_episodes: 2
  skip_rollout: true


env_name: halfcheetah
algo_name: polyflow
run_name: time_softmin


device: "cuda:0"
seed: 42

steps: 10  # 采样步数