defaults:
  - train_polyflow_walkercpx2
  - _self_

backbone:
  share_traj_encoder: true
  time_invariance_cons: true

env_name: walker2dcpx2
algo_name: polyflow
run_name: train_fixcons


device: "cuda:3"
seed: 42

steps: 10  # 采样步数