defaults:
  - train_polyflow_walkercpx_fixcons
  - _self_

algorithm:
  use_ot_batch: true


env_name: walker2dcpx
algo_name: polyflow
run_name: train_fixcons_ot


device: "cuda:3"
seed: 42

steps: 10  # 采样步数